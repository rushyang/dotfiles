require("toggles.CoC")                                          -- Toggling CoC in lua as couldn't find direct built-in toggle
require("toggles.focus")                                          -- Toggling CoC in lua as couldn't find direct built-in toggle
require("toggles.Lsp")
