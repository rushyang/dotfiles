require("toggles.CoC")                                          -- Toggling CoC in lua as couldn't find direct built-in toggle
require("toggles.golden_size")                                  -- Toggling Golden_Size via <F3> when using DiffView plugin
